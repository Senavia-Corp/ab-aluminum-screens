---
import VideoPage from '../../../components/pages/VideoPage.astro';
import { videoPaths } from '../../../lib/routes';
export async function getStaticPaths() {
  return videoPaths();
}
const { slug } = Astro.params;
---
<VideoPage lang="es" slug={slug as string} />
